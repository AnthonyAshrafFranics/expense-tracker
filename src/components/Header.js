import { Typography } from "@mui/material";
import React from "react";

export const Header = () => {
  return (
    <Typography variant="h2" style={{ fontSize: "2em" }} className="fw-bold ">
      Expense Tracker
    </Typography>
  );
};
